<%- include('partials/head') %>

    <body>
        <%- include('partials/navbar') %>


            <h1> The random number is : <%= num %>
                    <% if (num %2===0){ %>
                        <h2> That is an even number</h2>
                        <% } else { %>
                            <h2>thats an odd number</h2>
                            <% } %>
            </h1>
    </body>

    </html>